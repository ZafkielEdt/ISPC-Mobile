<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/routineItem"
        android:layout_width="395dp"
        android:layout_height="85dp"
        android:background="@drawable/abs"
        android:gravity="bottom"
        android:onClick="goToDescription"
        android:paddingStart="8dp"
        android:paddingBottom="4dp"
        android:text="TextView"
        android:textAlignment="gravity"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.025" />

    <ImageButton
        android:id="@+id/deleteBtn"
        android:layout_width="68dp"
        android:layout_height="82dp"
        android:backgroundTint="@color/transparent"
        android:contentDescription="delete button"
        app:layout_constraintBottom_toBottomOf="@+id/routineItem"
        app:layout_constraintEnd_toEndOf="@+id/routineItem"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/routineItem"
        app:layout_constraintVertical_bias="0.529"
        app:srcCompat="@android:drawable/ic_menu_close_clear_cancel" />

</androidx.constraintlayout.widget.ConstraintLayout>